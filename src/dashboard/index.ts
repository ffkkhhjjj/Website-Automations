/**
 * Dashboard module public surface.
 * - service: getOverview() — real-table overview computation
 * - routes: registerDashboardRoutes(app) — API + page routes
 * - types: payload shapes
 */
export * from './types';
export * from './service';
export * from './routes';